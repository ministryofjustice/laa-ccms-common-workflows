name: PACT test and publish

on:
  workflow_call:
    inputs:
      java_version:
        required: false
        type: string
        default: 21
      java_distribution:
        required: false
        type: string
        default: temurin
      pact_test_task:
        required: true
        type: string
      pacticipant_name:
        required: true
        type: string
      consumer_name:
        required: false
        type: string # Leave blank for testing all consumers
      provider_name:
        required: false
        type: string
      version:
        description: 'The version (SHA) of the pacticipant'
        required: false
        type: string
        default: ${{ github.sha }}
      publish_pact_results:
        description: 'Publish pact test results to Pact Broker (Consumer only)'
        required: false
        type: boolean
        default: false
      is_provider:
        description: 'Is this a provider?'
        required: false
        type: boolean
        default: false
      enable_can_i_merge:
        description: 'Enable Can I Merge?'
        required: false
        type: boolean
        default: false
      git_branch:
        description: 'Git Branch to record Pact against (defaults to either branch or tag name)'
        required: false
        type: string
        default: ${{ github.head_ref }}
      pact_tags:
        description: 'Comma-separated list of tags to add to Pact (e.g., "v1.0.0,production,stable")'
        required: false
        type: string

    secrets:
      gh_token:
        required: true
      pact_broker_url:
        required: false
      pact_broker_username:
        required: false
      pact_broker_password:
        required: false

permissions:
  contents: write
  packages: write

jobs:

  pact-test-and-publish:
    if: ${{ github.actor != 'dependabot[bot]' }}
    name: Pact tests
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.gh_token }}
      GIT_SHA: ${{ github.sha }}
      PACT_BROKER_URL: ${{ secrets.pact_broker_url }}
      PACT_BROKER_USERNAME: ${{ secrets.pact_broker_username }}
      PACT_BROKER_PASSWORD: ${{ secrets.pact_broker_password }}
    steps:
      - name: Set branch name
        run: |
          if [[ -n "${{ inputs.git_branch }}" ]]; then
            echo "GIT_BRANCH=${{ inputs.git_branch }}" >> $GITHUB_ENV
          else
            echo "GIT_BRANCH=${GITHUB_HEAD_REF:-${GITHUB_REF_NAME}}" >> $GITHUB_ENV
          fi

      - name: Set pact tags
        run: echo "PACT_TAGS=${{ inputs.pact_tags }}" >> $GITHUB_ENV

      - name: Get app token
        if: ${{ env.GITHUB_APP_ID != '' && env.GITHUB_APP_PRIVATE_KEY != '' }}
        id: get_workflow_token
        uses: peter-murray/workflow-application-token-action@d17e3a9a36850ea89f35db16c1067dd2b68ee343 # v4.0.1
        with:
          application_id: ${{ secrets.github_app_id }}
          application_private_key: ${{ secrets.github_app_private_key }}
          organization: ${{ secrets.github_app_organisation }}
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        if: ${{ inputs.create_tag != 'true' }}
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        if: ${{ inputs.create_tag == 'true' }}
        with:
          ref: ${{ inputs.override_tagged_branch }}
          token: ${{ steps.get_workflow_token.outputs.token || env.GITHUB_TOKEN }}
      - name: Set up JDK ${{ inputs.java_version }}
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          java-version: ${{ inputs.java_version }}
          distribution: ${{ inputs.java_distribution }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@39e147cb9de83bb9910b8ef8bd7fff0ee20fcd6f # v6.0.1

      - name: Run pact test
        id: run_pacts
        env:
          PACT_BROKER_USERNAME: ${{ secrets.PACT_BROKER_USERNAME }}
          PACT_BROKER_PASSWORD: ${{ secrets.PACT_BROKER_PASSWORD }}
          PACT_CONSUMER_NAME: ${{ inputs.consumer_name }}
        run: ./gradlew ${{ inputs.pact_test_task }}

      - name: Print branch environment name
        run: echo ${{ env.GIT_BRANCH }}

      - name: Publish pact test results
        if: ${{ inputs.publish_pact_results == true }}
        run: ./gradlew pactPublish --info

      - name: Can I merge?
        if: ${{ inputs.enable_can_i_merge == true }}
        uses: ministryofjustice/laa-ccms-common-workflows/.github/actions/pact-can-i-merge@v1
        with:
          pact_broker_password: ${{ secrets.PACT_BROKER_PASSWORD }}
          pact_broker_url: ${{ secrets.PACT_BROKER_URL }}
          pact_broker_username: ${{ secrets.PACT_BROKER_USERNAME }}
          pacticipant: ${{ inputs.pacticipant_name }}
          is_provider: ${{ inputs.is_provider }}
          provider_name: ${{ inputs.provider_name }}

      - name: Record version
        if: ${{ inputs.is_provider == true }}
        id: record_version
        shell: bash
        run: |
          docker run --rm \
            -e PACT_BROKER_BASE_URL=${{ secrets.PACT_BROKER_URL }} \
            -e PACT_BROKER_USERNAME=${{ secrets.PACT_BROKER_USERNAME }} \
            -e PACT_BROKER_PASSWORD=${{ secrets.PACT_BROKER_PASSWORD }} \
            pactfoundation/pact-cli:latest \
            broker create-or-update-version \
            --pacticipant ${{ inputs.pacticipant_name }} \
            --version ${{ inputs.version }} \
            --branch ${{ env.GIT_BRANCH }}
          
